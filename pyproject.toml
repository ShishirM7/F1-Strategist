[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "f1_strategy_assistant"
version = "0.1.0"
dependencies = [
    "fastf1",
    "pandas",
    "numpy"
]

[tool.setuptools.packages.find]
where = ["."]